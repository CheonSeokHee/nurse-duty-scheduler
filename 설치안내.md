# 🩺 간호사 3교대 듀티표 자동 생성기 — 설치 안내

Google 스프레드시트 + Apps Script 로 동작합니다.
**한 번만 세팅**하면, 그 뒤로는 메뉴 버튼만 누르면 됩니다.
설치·서버 불필요, 링크 공유 + 권한 설정만으로 어디서든(PC·폰) 접속·수정 가능합니다.

---

## 1. 빈 시트 만들기
1. <https://sheets.google.com> 접속 → **빈 스프레드시트** 새로 만들기
2. 파일 이름을 `간호사 듀티표` 등으로 변경

## 2. 스크립트 붙여넣기
1. 상단 메뉴 **확장 프로그램 → Apps Script** 클릭
2. 열린 편집기의 기존 `function myFunction() {}` 내용을 **전부 삭제**
3. 같은 폴더의 **`Code.gs`** 파일 내용을 **전부 복사 → 붙여넣기**
4. 💾 저장 (Ctrl/Cmd + S)

## 3. 시트로 돌아와 메뉴 띄우기
1. 스프레드시트 탭으로 돌아오기
2. **새로고침(F5)** → 잠시 후 상단에 **`🩺 듀티표`** 메뉴가 생깁니다
3. **`🩺 듀티표 → ① 시트 세팅(최초 1회)`** 클릭
4. 처음엔 **권한 승인** 창이 뜹니다 → 본인 계정 선택 →
   "고급 → 안전하지 않은 페이지로 이동" → **허용**
   *(내가 만든 스크립트라 안전합니다. 외부로 데이터가 나가지 않습니다.)*
5. `설정` 시트와 `듀티표` 시트가 자동 생성됩니다 ✅

---

## 4. 사용법
### A. 설정 입력 (`설정` 시트)
- **연도 / 월** 입력
- **필요인원**: Day 3 · Evening 3 · Night 2 (병동에 맞게 수정 가능)
- **간호사 목록**: 이름 + 역할(`차지`/`액팅`) 입력 (기본 12명 예시 제공)
- (선택) **요청오프**: 그 사람이 꼭 쉬어야 하는 날짜 — 예 `3,10,21`
- (선택) **요청근무**: 특정 날 특정 근무 고정 — 예 `D:5 / N:12`
- (선택) **전월 나이트(일)**: 지난달 나이트 일수 → 적게 한 사람이 이번 달 나이트 우선
  *(매달 듀티표의 `N계` 값을 다음 달 이 칸에 복사하면 공평하게 돌아갑니다)*

### B. 자동 배정
- **`🩺 듀티표 → ② 자동 배정 실행`** 클릭
- 규칙을 모두 지키는 한 달치 표가 자동 생성됩니다 (색상 자동)
- 생성 직후 자동으로 규칙 검사까지 실행됩니다

### C. 수기 수정 + 검사
- 칸을 직접 클릭해 `D / E / N / O` 로 자유롭게 수정 (드롭다운 제공)
- 수정 후 **`③ 규칙 검사 / 위반 표시`** 클릭
  - 하드규칙 위반 칸 → **빨간 배경**
  - 인원 부족·차지 없음·오프 개수 초과 등 → **팝업 목록**으로 안내

---

## 5. 적용된 규칙
| 항목 | 값 |
|---|---|
| 근무 종류 | Day / Evening / Night / Off |
| 하루 주간 인원 | **인원수에 맞춰 조절** (11명이면 D3·E2 = 5명, 12명이면 D3·E3 = 6명) |
| 나이트 | 하루 2명, 2~3일 블록 |
| 나이트 시작 전 | **3연속 블록만 1오프** (짧은 블록은 근무 후 바로 OK) |
| 나이트 종료 후 | **1오프** |
| 나이트 개수 | **액팅 6개 고정, 차지 = 나머지** (인원수 자동 계산) |
| 1인 오프 | **정확히 10~11개** (보정 패스로 보장) |
| 연속근무 | 최대 4일 (앞뒤 양방향) |
| 금지 패턴 | N 다음날 D/E,  E 다음날 D |
| 차지 규칙 | 모든 근무에 차지 ≥ 1명 (나이트 포함) |

> 위 값은 모두 `설정` 시트에서 숫자만 바꾸면 즉시 반영됩니다.

### 🔑 인원수와 주간 인원의 관계 (오프 10~11 맞추기)
오프를 10~11로 맞추려면 **주간 인원이 인원수에 따라 정해져요**:
- 한 달 일하는 양 = (인원수 × 20일). 거기서 나이트(60)를 빼면 주간 몫이 나옴.
- **11명 → 주간 하루 5명** (D3 E2 또는 D2 E3) / **12명 → 하루 6명** (D3 E3)
- 주간을 너무 많이 잡으면(11명에 D3E3) 일이 과해서 오프가 10 밑으로 떨어지고 빨간 빈칸이 생깁니다.

### 나이트 개수 자동 계산
나이트 자리 = 2명 × 30일 = 60개. 매 밤 차지 1명 필수 → 액팅이 먼저 6개씩 가져가고, 차지가 나머지를 나눠 가짐.
- **11명(차지6·액팅5): 액팅 전원 6 / 차지 3~6(평균 5) → 액팅 ≥ 차지 보장** ✅ (★ `나이트 분배 = 1` 로 둘 것!)
- 12명(6·6): 액팅·차지 모두 ~5 (액팅>차지 불가, 동률이 한계)
- 10명(5·5): 액팅 6 / 차지 6

> **11명에선 반드시 `나이트 분배 = 1`** 로 두세요. 그래야 액팅이 차지보다 적어지지 않아요.
> (차지가 일부 3·일부 6으로 갈리는 건 `전월 나이트` 칸으로 매달 번갈아 공평해집니다.)

### ⚠️ 참고 1 — 나이트 분배 방식 (설정의 `나이트 분배`)
나이트가 **하루 2명**이면 한 달 총 나이트는 `2 × 30 = 60일`, 12명이 나누면 **1인 평균 5일**.
**기본값 3(균등)** 으로 두면 **전원 2블록(≈5나이트)씩** 배정돼 "1번만 도는 사람"이 없어요.

| 값 | 방식 | 주간 빈칸(수기보정) | 나이트 분포 |
|---|---|---|---|
| 1 | 인원 우선 | 거의 0 | 6 또는 3 (캐리오버로 매달 번갈아) |
| 2 | 균형 | 적음 | 3~6 |
| **3** | **균등 (기본)** | 거의 0 (전 오프 0일 때) | **전원 4~6 (전원 2블록)** |

- **전 오프를 0**으로 두는 게 핵심이에요(근무 후 바로 나이트 OK). 이러면 균등(3)이어도 빈칸이 거의 안 생깁니다.
- 주간 빈칸은 `③ 규칙 검사`에서 빨갛게 표시 → 한 명씩 채우면 됩니다.
- **전월 나이트(일)** 칸을 채우면 매달 나이트가 적었던 사람이 우선 배정돼 장기적으로 공평해집니다.
- 전원 6일이 꼭 필요하면 → 일부 날 **Night 필요인원을 3명**으로 늘려야 합니다(오프는 9개로 감소).

### ⚠️ 참고 2 — 2월(28일)
28일 달은 1인당 오프가 약 9개로 떨어집니다 (28일 × 8근무 ÷ 12명 = 수학적 한계).
→ **2월엔 `월 오프 최소`를 `9`로 낮추세요.** 그러면 빈칸이 사라집니다.
(30·31일 달은 10 그대로 두면 됩니다.)

---

## 6. 다른 사람과 공유
- 우측 상단 **공유** → 같이 보는 사람 이메일 추가
- 보기만: **뷰어**, 같이 수정: **편집자** 권한 부여
- 메뉴 `🩺 듀티표`는 **편집자** 권한이 있어야 보입니다
  *(처음 여는 사람은 한 번 권한 승인 필요)*
