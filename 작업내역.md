# 작업 내역

## 최근 작업 (11명 병동 대응)
1. **인원 11명** 기본 명단 (차지6/액팅5)
2. **데이2·이브닝2 고정 + 남는 액팅 S(보조근무)** — 인원이 남는 구성을 자동 감지(surplus)해서, 차지는 정규 D/E로 오프를 맞추고 남는 액팅은 S로 돌려 오프 10~11 유지.
3. **사람별 나이트 최대연속** — 명단에 `나이트최대` 열(편혜경·박수진은 2 = 한 번에 1~2개씩).
4. **듀티개수 입력**(`D:14/N:5/E:0`) = 특정 날짜가 아니라 한 달 **개수** 목표. 특정 날짜 근무는 표에 직접 입력(자동 잠금).

## 그 이전 작업
- 간호사별 **선호 듀티 + 강도**(약간/보통/강하게), 극단 쏠림 방지.
- **첫 요청오프 전날=Day / 다음날=나이트(D-O-N)** 규칙 (수기 입력한 O도 인식).
- **수기 입력 칸 자동 잠금** — 재실행해도 직접 고친 칸 유지.
- **하드 위반 안정화** — 금지패턴(E→D 등)·나이트 전후오프 점수화 + 최종 안전망. 퍼즈 1000케이스 위반 0.
- **시간예산 적응형 탐색** — 검증 통과 표 찾을 때까지 자동으로 더 굴림(쉬운 달은 즉시 종료).
- **clasp 자동 배포** — 코드 수정 시 시트에 바로 반영.
